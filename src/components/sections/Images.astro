---
import { Image } from "astro:assets";
import Section from "../Section.astro";

import graphicDesignImages from "../../assets/graphic-design";
const images = graphicDesignImages;

// import CV from "@cv";
// const images = CV.images!;
---

<Section className={Astro.props.className} title="Graphic Design Gallery">
  {
    images?.intro && 
    <p class=" ml-3 text-skin-muted">
      {images.intro} 
      <span class="italic text-sm text-skin-muted/75 underline"> 
        (click to see full size)
      </span>  
    </p>}
  <div
    class="grid grid-cols-1 gap-2 p-2 pt-1 sm:grid-cols-2 md:grid-cols-3 lg:grid-cols-4"
  >
    {
      images.list.map(({ image, alt, desc }) => (
       <div class="relative overflow-hidden rounded-lg border border-skin-hue/20 bg-skin-button-accent/20" style="height: 0; padding-bottom: 100%;">
          <a
            href={image.src}
            data-fancybox="gallery"
            data-caption={desc}
            class="block aspect-square"
          >
            <Image
              src={image}
              alt={alt}
              width={500}
              height={500}
              class="h-full w-full object-cover transition-transform duration-300 hover:scale-105"
            />
          </a>
        </div>
      ))
    }
  </div>
</Section>

<script>
  import { Fancybox } from "@fancyapps/ui";
  Fancybox.bind('[data-fancybox="gallery"]', { hideScrollbar: false });
</script>